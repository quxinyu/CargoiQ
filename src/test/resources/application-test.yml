# GraphDB 集成测试配置
graphdb:
  # 使用 HTTP 远程 GraphDB 服务器
  repository-type: http
  http:
    # GraphDB 服务器地址（通过 docker-compose 启动）
    url: http://192.168.6.59:7200/repositories/neone
    # GraphDB Free 版本默认不需要认证
    username:
    password:

spring:
  profiles:
    active: test
